功能: 端到端测试4
  背景: 分析结果来自当前需求文档
    假如 已加载当前任务的需求说明

  # priority: P2
  场景: `POST /api/tasks`
    假如 当前平台已进入联调阶段，研发、测试与前端需要基于统一口径确认以下事项：平台主流程是否可在同一任务生命周期内闭环完成；关键中间产物是否可追踪、可复核、可回放；执行与分析结果是否满足演示与验收所需的最小可信度。
    当 `POST /api/tasks`
    那么 平台主流程可在同一任务生命周期内闭环完成

  # priority: P2
  场景: `GET /api/tasks/{task_id}`
    假如 当前平台已进入联调阶段，研发、测试与前端需要基于统一口径确认以下事项：平台主流程是否可在同一任务生命周期内闭环完成；关键中间产物是否可追踪、可复核、可回放；执行与分析结果是否满足演示与验收所需的最小可信度。
    当 `GET /api/tasks/{task_id}`
    那么 关键中间产物可追踪、可复核、可回放
